/**
 * Where this copy of the source lives.
 *
 * musicbutler is AGPL-3.0. Section 13 says a user who reaches the app over a
 * network must be offered its source, so the sidebar links here. Anyone who
 * runs a changed copy has to point this at their own version.
 */
export const SOURCE_URL =
	import.meta.env.VITE_SOURCE_URL ?? "https://github.com/andreazllin/musicbutler";
